{
  "name": "ironledger",
  "version": "0.1.0",
  "private": true,
  "description": "IronLedger smart contract design draft for CSE 540",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "gas-report": "REPORT_GAS=true hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "hardhat-gas-reporter": "^1.0.10"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "dotenv": "^17.3.1"
  }
}
